# 输出格式规范

## Markdown

使用标准 Markdown 标题、列表、引用与图片语法。保留结构语义，便于归档和二次编辑。

## HTML

输出可独立保存的语义化正文片段。使用内联样式或简单稳定的标签；不得依赖 JavaScript。正文宽度和字号适合移动阅读，并为图片提供 `alt`。

## 微信可复制版

优先使用微信编辑器通常可保留的标题、段落、加粗、引用、列表和分隔效果。避免复杂网格、表格、外部字体、定位、动画与外链样式表。

## 输出顺序

1. 标题与摘要
2. 正文
3. 图片清单
4. 审核状态与人工确认项
5. 用户要求的格式版本
